{ 
  "name": "lifelens-app-workspace", 
  "version": "0.0.0", 
  "private": true, 
  "license": "MIT", 
  "scripts": { 
    "build": "pnpm run typecheck && pnpm -r --if-present run build", 
    "typecheck:libs": "tsc --build", 
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck" 
  }, 
  "pnpm": { 
    "onlyBuiltDependencies": ["esbuild", "protobufjs", "@google/genai", "sharp", "canvas"] 
  }, 
  "devDependencies": { 
    "prettier": "^3.8.4", 
    "typescript": "~5.9.3" 
  } 
}
